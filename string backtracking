/*public class String_backtrack {
    public static void subStringtrace(String str,int index){
        if(str.length()==0){
            System.out.println(str);
            return;
        }
        for(int i=0;i<str.length();i++){
            String s=str.charAt(i);
            String result=s.subString(0,i)*s.subString(i+1);
            subStringtrace(result, )


            
        }
    }
    public static void main(String args[]){
        String str="ABC";
        subStringtrace(str," ");
    }

    
}
*/
public class String_backtrack {

    public static void subStringtrace(String str, String ans) {

        // Base case
        if (str.length() == 0) {
            System.out.println(ans);
            return;
        }

        for (int i = 0; i < str.length(); i++) {

            char ch = str.charAt(i);

            // remove the chosen character
            String result = str.substring(0, i) + str.substring(i + 1);

            subStringtrace(result, ans + ch);
        }
    }

    public static void main(String args[]) {
        String str = "ABC";
        subStringtrace(str, "");
    }
}
